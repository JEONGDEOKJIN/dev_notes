```tsx

const selected = watch(name) // 여기에서 name 은 caner 일 것 

// 프로그램과 옵션에 따라 다르므로, 공통화! 
const LABEL = 
"mofAnpExmnNm" in props 
? props.molaAPNTeXMNnM
:  props.exmnCdNm

const isIncludes = selected.some( (v) => v.label === LABEL ) 

여기에서 

```

### 📌 코드 분석

```tsx

const selected = watch(name); // name === "caner" 일 수 있음

const LABEL = "mofAnpExmnNm" in props
  ? props.molaAPNTeXMNnM
  : props.exmnCdNm;

const isIncludes = selected.some((v) => v.label === LABEL);

```
